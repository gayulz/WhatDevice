# E2E Scenarios — WhatDevice

> Auto-generated by `/auto setup`. Updated by `/auto sync`.

## Project Type: Frontend (Static Site) + Build (Java/Gradle)
## Binary: N/A (정적 사이트)
## Build: `./gradlew run`

---

### S1: build — 전체 빌드 성공
- **Command**: `./gradlew run`
- **Precondition**: Java 21 설치, `gradle wrapper --gradle-version 8.10.2` 1회 실행 완료 (gradle-wrapper.jar 생성)
- **Env**: N/A
- **Expect**: 빌드 성공, `dist/` 디렉터리 재생성, 기기 페이지 160개 + 가이드 2개 + 정책 3개 + index.html + data.js + sitemap.xml + robots.txt 생성
- **Verify**: `exit_code(0)`, `file_exists(dist/index.html)`, `file_exists(dist/data.js)`, `file_exists(dist/sitemap.xml)`, 디렉터리 파일 수 `dist/device/*.html >= 150`
- **Depends**: N/A
- **Status**: active

### S2: build_idempotent — 재빌드 멱등성
- **Command**: `./gradlew run && ./gradlew run`
- **Precondition**: S1 통과
- **Env**: N/A
- **Expect**: 두 번째 빌드도 성공, `dist/` 내용물이 첫 빌드와 동일 (cleanDist task가 dist를 비우고 재생성)
- **Verify**: `exit_code(0)`, `dist/index.html` 해시가 두 빌드에서 동일
- **Depends**: S1
- **Status**: active

### S3: search_substring — 식별자 substring 검색
- **Command**: 브라우저에서 `/index.html?q=iPhone18` 열기
- **Precondition**: dist 빌드 완료, 로컬 서버 또는 file:// 로 접근 가능
- **Env**: N/A
- **Expect**: 검색 입력란에 "iPhone18"이 채워지고, `iPhone18,1`, `iPhone18,2` 등 iPhone18 계열 기기 카드만 결과로 표시
- **Verify**: `element_visible(.search-results)`, `element_text(.search-results, "iPhone18")`, 결과 카드 개수 >= 1
- **Depends**: S1
- **Status**: active

### S4: search_empty — 빈 검색어 전체 표시
- **Command**: 브라우저에서 `/index.html` 열기 (검색 입력 비움)
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: 검색 결과 영역에 전체 160개 기기 카드 표시 (탐색 모드)
- **Verify**: `element_visible(.search-results)`, 결과 카드 개수 == 160
- **Depends**: S1
- **Status**: active

### S5: search_korean — 한글 검색
- **Command**: 브라우저에서 `/index.html?q=아이폰` 열기
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: 한글 표기에 "아이폰"이 포함된 모든 기기가 결과로 표시
- **Verify**: `element_visible(.search-results)`, 결과 카드 개수 >= 1
- **Depends**: S1
- **Status**: active

### S6: device_page — 기기 상세 페이지 렌더
- **Command**: 브라우저에서 `/device/iphone18-1.html` 열기
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: 페이지 제목에 식별자 포함, 정식 모델명·계열 정보·카테고리 표시, canonical 메타태그 정상
- **Verify**: `page_title("iPhone18,1")`, `element_visible(.device-detail)`, `<link rel="canonical">` href 존재
- **Depends**: S1
- **Status**: active

### S7: guide_page — 가이드 페이지 렌더
- **Command**: 브라우저에서 `/guide/identifier-basics.html` 열기
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: 가이드 본문 표시, 헤더 네비게이션 정상, 다크모드 토글 노출
- **Verify**: `element_visible(article)`, `element_visible(.theme-toggle)`
- **Depends**: S1
- **Status**: active

### S8: dark_mode_toggle — 다크모드 토글 영속화
- **Command**: 브라우저에서 `/index.html` 열기 → 다크모드 토글 클릭 → 페이지 새로고침
- **Precondition**: dist 빌드 완료, localStorage 접근 가능
- **Env**: N/A
- **Expect**: 새로고침 후에도 다크모드 유지 (FOUC 없이 즉시 다크 적용)
- **Verify**: `<html>` 요소에 `dark` 클래스 적용, `localStorage.getItem("theme") == "dark"`
- **Depends**: S1
- **Status**: active

### S9: responsive_mobile — 모바일 뷰포트 반응형
- **Command**: 뷰포트 375x667로 `/index.html` 열기
- **Precondition**: dist 빌드 완료
- **Env**: viewport=mobile
- **Expect**: 검색 그리드가 1열로 축소, 폰트·여백이 모바일에 맞게 조정, 가로 스크롤 없음
- **Verify**: `element_visible(.search-input)`, body의 가로 overflow 없음
- **Depends**: S1
- **Status**: active

### S10: sitemap_valid — sitemap.xml 검증
- **Command**: `/sitemap.xml` 파일 읽기
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: XML 형식 유효, `<url>` 항목 >= 165개 (기기 160 + 가이드 2 + 정책 3 + 인덱스 1 = 166 내외)
- **Verify**: `file_exists(dist/sitemap.xml)`, XML parse 성공, `<url>` 개수 >= 160
- **Depends**: S1
- **Status**: active

### S11: robots_valid — robots.txt 검증
- **Command**: `/robots.txt` 파일 읽기
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: `Sitemap:` 줄 포함, `User-agent: *` + `Allow: /` 또는 적절한 정책 포함
- **Verify**: `file_exists(dist/robots.txt)`, `file_contains(dist/robots.txt, "Sitemap:")`
- **Depends**: S1
- **Status**: active

### S12: data_js_global — data.js 전역 배열 유효성
- **Command**: 브라우저에서 `/index.html` 열고 콘솔에서 `window.DEVICES.length` 확인
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: `window.DEVICES`가 배열이고 길이 == 160, 각 항목이 `{i, n, k, s, c}` 키 모두 보유
- **Verify**: console.log("DEVICES.length", window.DEVICES.length) == 160, 첫 항목에 `i`, `n`, `k`, `s`, `c` 키 존재
- **Depends**: S1
- **Status**: active

### S13: identifier_collision — 슬러그 충돌 회귀
- **Command**: `dist/device/` 디렉터리에서 중복 파일명 검사
- **Precondition**: dist 빌드 완료
- **Env**: N/A
- **Expect**: 모든 슬러그가 유일 — 파일명 중복 없음 (`iPad14,8`과 `iPad14,8-A`처럼 변형 식별자가 같은 슬러그로 변환되지 않아야 함)
- **Verify**: `ls dist/device/*.html | sort -u | wc -l == ls dist/device/*.html | wc -l`
- **Depends**: S1
- **Status**: active

### S14: site_url_replaced — 배포 전 SITE_URL 교체 확인
- **Command**: `grep -r "YOUR-USERNAME" dist/` 실행
- **Precondition**: dist 빌드 완료, 배포 직전 시점
- **Env**: N/A
- **Expect**: `YOUR-USERNAME` 문자열이 `dist/` 어디에도 등장하지 않음 (SITE_URL 상수가 실제 사용자명으로 교체된 후 재빌드된 상태)
- **Verify**: `exit_code(1)` (grep 미스가 정상)
- **Depends**: S1
- **Status**: active (배포 직전 게이트)
