# Canary Configuration — WhatDevice

> Auto-generated by `/auto setup`. Updated by `/auto sync`.

## Project Type: Frontend (Static Site) + Build (Java/Gradle)
## Build: `./gradlew run`
## Deploy Platform: GitHub Pages (gh-pages branch, 현재 미활성)

---

## Health Checks

| ID | Type | Target | Expect | Timeout |
|----|------|--------|--------|---------|
| H1 | build | `./gradlew run` | exit 0, `dist/` 생성 | 120s |
| H2 | file | `dist/index.html` | 파일 존재, 크기 > 1KB | 5s |
| H3 | file | `dist/data.js` | 파일 존재, `window.DEVICES` 변수 포함 | 5s |
| H4 | file | `dist/sitemap.xml` | XML parse 성공, `<url>` 항목 >= 160 | 5s |
| H5 | file | `dist/robots.txt` | `Sitemap:` 라인 포함 | 5s |
| H6 | dir-count | `dist/device/*.html` | 파일 개수 >= 150 | 5s |
| H7 | grep | `grep -r "YOUR-USERNAME" dist/` | exit 1 (미스 정상) — placeholder 교체 확인 | 5s |
| H8 | browser | 배포 URL `/` | 페이지 로드 OK, 콘솔 에러 0 | 10s |
| H9 | browser | 배포 URL `/device/iphone18-1.html` | 200 OK, canonical 메타 정상 | 10s |
| H10 | browser | 배포 URL `/sitemap.xml` | 200 OK, Content-Type: application/xml | 5s |

---

## Detection Sources

- **Build**: `build.gradle`의 `application` 플러그인 + `mainClass = "com.whatdevice.BuildSite"`
- **Health endpoint**: 없음 (정적 사이트 — HTTP 엔드포인트 대신 파일 존재 + 브라우저 체크로 대체)
- **Browser targets**: `dist/index.html` (메인), `dist/device/iphone18-1.html` (대표 상세 페이지), `dist/sitemap.xml` (SEO)
- **Deploy platform**: `.github/workflows/build.yml.disabled`의 publish target 추정 (현재 비활성)

---

## Verdict Rules

- **PASS**: H1~H7 모두 통과 + 배포된 경우 H8~H10 모두 통과
- **WARN**: H1~H6 통과 + H7 실패 (`YOUR-USERNAME` 잔존 — 배포 전 경고)
- **WARN**: 빌드 통과 + 일부 브라우저 체크에서 콘솔 경고 (404 없음)
- **FAIL**: H1 (빌드) 실패 OR H2~H6 (필수 파일) 누락 OR H8 (메인 페이지) 로드 실패

---

## Browser Health Targets (배포 후)

배포 URL이 결정된 후 다음을 `--url` 옵션으로 검사:

```bash
auto canary --url https://{username}.github.io/whatdevice/
```

검사 대상 페이지:
1. `/` — 메인 검색 페이지, 검색 입력란·다크모드 토글·기기 카드 그리드 노출
2. `/device/iphone18-1.html` — 대표 기기 상세 페이지, 헤더·본문·계열 정보 렌더
3. `/guide/identifier-basics.html` — 가이드 페이지, 본문 텍스트 노출
4. `/sitemap.xml` — XML 응답, Content-Type 확인
5. `/robots.txt` — 텍스트 응답, `Sitemap:` 라인 확인

---

## 배포 전 필수 체크리스트

H7 (SITE_URL 교체) 외에 다음을 수동 확인:

- [ ] `BuildSite.java:43` `SITE_URL`이 실제 GitHub Pages URL로 교체됨
- [ ] `./gradlew run` 재실행하여 `dist/` 내 canonical, og:url, sitemap.xml 갱신됨
- [ ] `grep -r "YOUR-USERNAME" dist/` → 결과 없음
- [ ] `.github/workflows/build.yml.disabled` → `build.yml`로 리네임 (자동 배포 활성화)
- [ ] GitHub 저장소 Settings → Pages → Source 설정 완료
- [ ] 첫 배포 후 H8~H10 통과 확인
